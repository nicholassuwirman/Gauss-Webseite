import React from "react";
import "./becomePartner.css"; // Linking the CSS file

const BecomePartner = () => {
  return (
    <div className="become-container">
      {/* Add content here */}
    </div>
  );
};

export default BecomePartner;
